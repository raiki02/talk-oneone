import QtQuick

ListModel{
    id:listModel
    ListElement{
        context:""
        role:""
    }

    // ListElement{
    //     context:"deontology：The core of deontology is obligation and moral rules"
    //     role:"Assistant"
    // }

    // ListElement{
    //     context:"Each Rectangle item is painted using either a solid fill color, specified using the color property, or a gradient, defined using a Gradient type and set using the gradient property. If both a color and a gradient are specified, the gradient is used."
    //     role:"User"
    // }
}
